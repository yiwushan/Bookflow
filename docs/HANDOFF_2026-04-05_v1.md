# BookFlow Handoff (2026-04-05)

## 1) 当前状态
- 仓库路径: `/home/wushanyi/playground/bookflow`
- 当前分支: `v1-dev`
- 关键提交:
  - `e66afd7` release: BookFlow v0.1.0 (TOC-driven PDF feed MVP)
  - `14fbcd4` feat(v1): add idle derived-pdf cleanup queue and workflow hardening
- 工作区状态:
  - 已跟踪文件干净（仅有未跟踪 `.codex`，可忽略）

## 2) 本轮已完成（v1 第一项）
- 增加“闲时队列”自动清理废弃切片 PDF（orphan derived files）。
- 清理触发点已接入:
  - `TOC 保存并物化` 后入队
  - `导入任务(import_start)` 完成后入队
  - `同步导入(v1/books/import)` 完成后入队
  - `启动自举导入` 完成后入队
- 增加周期扫描兜底:
  - 即使漏入队，也会扫描 `data/books/derived/*` 并清理未被 `book_chunks.metadata.section_pdf_relpath` 引用的 PDF。
- 作业流程加固:
  - 防止并发导入踩踏：导入进行中再次导入会返回 `IMPORT_ALREADY_RUNNING`。
  - `/health` 返回加入 `derived_cleanup` 状态。
  - `/health` 的 `startup_bootstrap.last_summary` 改为紧凑摘要（避免返回过大）。

## 3) 关键代码位置
- 清理队列/线程/状态:
  - `server/app.py`
  - 关键词: `DERIVED_CLEANUP_*`, `_schedule_derived_cleanup`, `run_derived_cleanup_once`, `derived_cleanup_loop`
- 物化后入队:
  - `BookFlowStore.materialize_from_toc()` in `server/app.py`
- 导入后入队:
  - `_run_import_job()`, `_run_startup_import_cycle()`, `handle_books_import()` in `server/app.py`
- 并发导入保护:
  - `_find_running_import_job_id()` + `handle_books_import_start()` + `handle_books_import()`

## 4) OnePlus6T 部署与运行
- 设备项目路径: `/home/athbe/playground/bookflow`
- 启动命令（postgres + env）:
```bash
set -a && source /home/athbe/playground/bookflow/config/bookflow.device.env && set +a
/home/athbe/playground/bookflow/.venv/bin/python /home/athbe/playground/bookflow/server/app.py --host 0.0.0.0 --port 8000
```
- 当前可用入口:
  - `http://10.16.12.170:8000/app`
  - `http://10.16.12.170:8000/app/toc`
- 健康检查:
```bash
curl --noproxy '*' -sS http://127.0.0.1:8000/health
```

## 5) 已知注意点
- 如果本机有代理（`http_proxy`），访问局域网地址可能被代理拦截；测试时建议 `curl --noproxy '*' ...`。
- `tests/test_api_contract.py` 属历史契约，当前代码基线与其不完全一致；本轮变更未按该历史契约回填。
- v0 功能验收以 smoke 为主:
```bash
./scripts/smoke_first_product.sh --base-url http://127.0.0.1:8000 --token local-dev-token
```

## 6) 下一步建议（v1）
- 互动计数前端实时刷新（点赞/评论/完成后局部刷新卡片与阅读页统计）。
- 导入错误可视化增强（失败文件分组、可重试子集、错误原因更结构化）。
- 废弃切片清理补充管理接口（手动触发/查看最近批次详情）。

## 7) 新对话接续指令（可直接粘贴）
- “请读取 `/home/wushanyi/playground/bookflow/docs/HANDOFF_2026-04-05_v1.md`，在 `v1-dev` 分支继续推进 BookFlow v1，先做互动计数实时刷新。”

